#include <stdio.h>

int main() {
    int x=66;
    int y=8;
    int so=x+y;
    int sub=x-y;
    int mu=x*y;
    int di=x/y;
    printf("A soma, a subtracao, a multiplicacao e a divisao dos dois numeros e: %i, %i, %i, %i", so, sub, mu, di);

    return 0;
}