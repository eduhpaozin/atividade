#include <stdio.h>

int main() {
     float x=86;
     float y=43;
     float w=3;
     float so=x+y+w;
     float sub=x-y-w;
     float mu=x*y*w;
    float di=x/y/w;
    printf("A soma, a subtracao, a multiplicacao e a divisao dos tres numeros e: %f, %f, %f, %f", so, sub, mu, di);

    return 0;
} 